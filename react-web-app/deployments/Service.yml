apiVersion: v1
kind: Service
metadata:
  labels:
    app: "${{ values.name }}"
  name: "${{ values.name }}"
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 3000
  selector:
    deployment: "${{ values.name }}"
